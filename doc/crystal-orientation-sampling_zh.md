# 晶体取向采样

本文档描述 Lumice 如何在蒙特卡洛（Monte Carlo）冰晕模拟中对晶体 c 轴取向进行采样。

## 1. 物理模型

大气中的冰晶在气动力矩（aerodynamic torque）与随机扰动（stochastic perturbation）的共同作用下确定取向。单个六棱柱晶体的取向由三个角度完全描述：

- **天顶角（zenith angle, θ）**：晶体 c 轴与竖直方向之间的夹角，是决定晕形几何形态的主要因素。
- **方位角（azimuth angle, φ）**：c 轴投影绕竖直方向的旋转角。对于自由下落的晶体，此角服从均匀分布。
- **自转角（roll angle, ψ）**：晶体绕自身 c 轴的旋转角。

这种分解具有物理动机：重力定义了一个优选方向（竖直方向），而气动阻力主要沿此方向作用。由此产生的取向球面上的概率分布是一个**纬度带（zonal band）**——集中在某一优选余纬度附近，方位角均匀分布。这与旋转对称的球冠（cap）有本质区别：对称轴是重力矢量，而非晶体的 c 轴。这正是 von Mises–Fisher（vMF）分布不适用的原因——vMF 产生的是球冠而非纬度带。


## 2. 冰晶运动模式与分布选择

天顶角分布取决于雷诺数（Reynolds number, Re），而雷诺数由晶体尺寸、形状和下落速度决定。存在三种不同的运动状态，每种需要不同的统计模型。

### 2.1 稳定模式（低 Re）

在低雷诺数下，气动力矩将晶体恢复至平衡取向（板状晶体的 c 轴趋向竖直，柱状晶体的 c 轴趋向水平）。布朗运动和大气湍流引入小角度涨落。所得倾斜角服从**高斯分布（Gaussian distribution）**：

```
p(θ) ∝ exp(-(θ - μ)² / 2σ²)
```

其中 σ 通常为 0.4°–3°。偏振激光雷达反演结果表明，良好取向的板状晶体 σ ≈ 1°–2°（Noel & Sassen 2005），卫星观测也证实约 80% 的取向晶体偏离水平方向不超过 3°（Noel & Chepfer 2004）。

这是产生所有可辨认晕现象的运动状态：幻日（sundogs）、光柱（light pillars）、环天顶弧（circumzenith arcs）和幻日环（parhelic circles）。一个关键认识涉及**物理自洽性（physical self-consistency）**：产生可辨认晕的晶体*必须*具有高质量的取向（σ ≤ 数度），这恰好对应于高斯分布精确成立的稳定模式。锐利的晕需要窄的倾斜分布，而窄的倾斜分布仅在恢复力矩占主导时才会出现。这并非巧合，而是同一物理过程同时控制运动和光学的必然结果。

### 2.2 不稳定模式（高 Re）

当雷诺数超过临界值（六角板状晶体 Re > ~237）时，尾流变得不稳定，晶体进入振荡或混沌运动。

**之字形振荡（zigzag oscillation）。** 晶体来回摆动，倾斜角为：

```
θ(t) = |A · sin(ωt) + B|
```

其中 A 为振荡幅度，B 为平均倾斜偏移量。时间平均分布为**折叠反正弦分布（folded arcsine）**——呈 U 型，峰值出现在转折点附近。Stout 等人（2024）通过实验证实，高斯模型在这些不稳定模式下从定性上就是失败的。

**螺旋运动（spiral motion）。** 晶体绕竖直方向进动，倾斜角相对稳定。可以用均值偏移的高斯分布（μ ≠ 0）近似。

这两种模式产生的是弥散的背景光，而非锐利的晕。柱状晶体可能表现出特征性的倾斜角；Zhong 等人（2023）报告柱状晶体倾斜模式的 θ_e ≈ 38°。

### 2.3 尺寸聚合倾斜（size-aggregated tilt）

真实冰云中的晶体跨越一系列尺寸。对每种尺寸，σ 由 Re 决定；晶体越大，σ 越大。当倾斜角在完整尺寸分布上积分后，结果服从**拉普拉斯分布（Laplace / double-exponential distribution）**：

```
p(θ) ∝ exp(-|θ - μ| / b)
```

推导过程：若 X|σ ~ N(0, σ²) 且 σ² ~ Exp(λ)，则 X ~ Laplace(0, b)，其中 b = 1/√(2λ)。小晶体贡献锐利的高斯核心，大晶体贡献更重的指数尾部。

深空闪光观测证实了这一预测。Kostinski、Marshak 与 Várnai（2025）发现角衰减遵循 exp(−δ/s)，这恰好是拉普拉斯特征，数据来自 DSCOVR 航天器。

拉普拉斯分布简化了配置：无需使用多个具有不同高斯 σ 值的晶体条目，只需一个具有拉普拉斯尺度参数的条目即可。


## 3. 支持的分布类型

支持四种分布类型以及一种确定性模式。所有类型在 JSON 中共享相同的 `mean`/`std` 参数字段；`std` 的含义因类型而异。

| 类型 | 定义 | `mean` | `std` | 适用场景 |
|------|------|--------|-------|----------|
| `gauss` | p(θ) ∝ exp(−(θ−μ)²/2σ²) | 中心值 μ（度） | 标准差 σ（度） | 稳定模式取向 |
| `uniform` | p(θ) = 常数，定义域 [μ−w/2, μ+w/2] | 中心值 μ（度） | 全宽 w（度） | 随机取向；自转角 |
| `zigzag` | θ = \|A·sin(2πU) + B\|，折叠反正弦 | 倾斜偏移 B（度） | 幅度 A（度） | 大晶体振荡 |
| `laplacian` | p(θ) ∝ exp(−\|θ−μ\|/b) | 中心值 μ（度） | 尺度参数 b（度） | 尺寸聚合倾斜 |

JSON 中的**标量值**（如 `"zenith": 90`）视为确定性模式：每个晶体均接收该精确角度，不引入随机化。

**参数说明：**
- `gauss` σ = 2° 意味着 68% 的晶体落在均值 ±2° 范围内。
- `uniform` 的 `std` 是全范围宽度，而非标准差。
- `zigzag` 分布在折叠后的支撑集（support）为 [0, |A|+|B|]。
- `laplacian` 的标准差为 √2·b ≈ 1.41b。


## 4. 采样实现

### 4.1 雅可比校正（Jacobian correction）

在球面上采样角度 θ 需要考虑球面面积元素 sin(θ)。目标密度为：

```
p_sphere(θ) ∝ p_distribution(θ) · sin(θ)
```

若不做此校正，采样会过度集中在 sin(θ) → 0 的极点附近。

### 4.2 采样路径

`lat_path::SelectLatPath`（`src/core/shared/lat_path_selection.hpp`）是路径选择的单一权威源，被 CPU 采样器（`math.cpp::SampleSphericalPointsSph`）与两个 GPU 后端共用。它在四条路径中选择其一：

**路径 1：全天球（`kFullSphere`）。** 当轴分布是全天球均匀分布时触发（`AxisDistribution::IsFullSphereUniform()`：方位角与纬度均为全 360° 的 `uniform`，且自转角绕轴旋转对称）。纬度直接按面积测度采样——θ = asin(u)，u ~ U(-1,1)——给出球面均匀分布，无需拒绝。精确，O(1)。

**路径 2：确定性（`kNoRandom`）。** 当纬度分布类型为 `kNoRandom` 时触发（JSON 中的标量 `"zenith"` 值）。无需采样——每个晶体获得相同角度。

**路径 3：遗留高斯（`kGaussLegacy`）。** 当纬度分布类型为 `kGaussianLegacy` 时触发（JSON 中的 `"gauss_legacy"`）。此路径有意跳过 §4.1 描述的 sin(θ) 面积测度加权——仅用于复现雅可比修复（§5.1）之前的仿真结果，不建议在新配置中使用。

**路径 4：统一 LUT（`kLutInverseCdf`）。** 其余所有非退化类型——`kGaussian`、`kLaplacian`、`kUniform`、`kZigzag`——均走此路径。一张预计算的逆 CDF 查找表取代了此前三条独立的按分布采样器（瑞利 / 通用拒绝 / 确定性——已于 330.3 退役，见 §5.2）。详见 §4.3。

以上四条路径即当前完整的分类：不再有近极/远极的区分，本清单中也不再有任何拒绝循环（退役记录见 `doc/near-pole-area-measure-sampling.md`）。

### 4.3 统一逆 CDF LUT（`kLutInverseCdf`）

构建（host 侧，每个轴分布仅构建一次——`lat_lut.cpp::BuildLatLut`，由 `GetSharedLatLut` 在 CPU 工作线程与两个 GPU 后端间共享缓存，绝不逐光线重建）：

- 确定性求积（deterministic quadrature）计算每族分布的精确候选密度，经采样器自身所用的同一个 `normalize_latitude` 折叠处理，并按 sin(θ)——即 §4.1 的球面面积雅可比——加权，产生 `LatLut::kNodes = 257` 个等间隔余纬度节点及其严格递增的 CDF。
- 逐 bin 的 `flip_prob` 记录该 bin 内样本越过极点、需要将方位角/自转角翻转 π 的概率，以保持近极候选的对称性。

采样（一次均匀抽样，无拒绝循环）：

1. 抽取 ξ ~ U(0,1)。
2. `invert_lat_lut` 用固定 8 步二分查找加线性插值在 CDF 上定位 ξ（256 个区间 = 2⁸，因此查找开销恒定）。
3. 查表得到所在 bin 的 `flip_prob`，按该概率将方位角/自转角翻转 π。

此路径无拒绝：`LatPathDecision` 上的 `rejection_m` 字段（及镜像它的、已冻结的 device wire struct）在全部四条路径上均恒为 `1.0`。精度由节点数决定，而非包络常数——该查找表精确到节点分辨率误差，而非带拒绝尾部的近似。

### 4.4 复杂度

四条路径每个样本均为 O(1)，都不含循环。区别在于各自"精确"的含义：

| 路径 | 开销 | 精确性 |
|------|------|--------|
| `kFullSphere` | 一次 `asin` | 精确（闭式解） |
| `kNoRandom` | 无 | 精确（无分布） |
| `kGaussLegacy` | 一次高斯抽样 | 有意不做面积校正——复现修复前的遗留行为 |
| `kLutInverseCdf` | 一次均匀抽样 + 固定 8 步二分查找 | 精确到 `LatLut` 节点分辨率（257 个节点） |

已无接受率可言：此表此前描述的按分布拒绝循环（§5.2），以及促使其被替换的近极接受率崩溃问题，均已不复存在。


## 5. 历史背景

### 5.1 缺失的雅可比校正（修复前）

原始实现直接从分布中采样天顶角，未做 sin(θ) 雅可比校正。误差大小取决于平均天顶角：

| 平均天顶角 | 畸变倍率 | 受影响的类型 |
|-----------|---------|-------------|
| 90° | 1.0× | 柱状晶体晕 |
| 45° | 1.4× | — |
| 10° | 5.8× | 接近竖直的板状晶体晕 |
| 0° | ∞ | 完美取向的板状晶体 |

该修复于 2026-04 实现，同期的评估还考察并否决了 vMF、Matrix Fisher 和 Bingham 分布作为替代方案。

### 5.2 已退役的按分布包络/拒绝架构（330.3 之前）

在雅可比修复（§5.1）之后、330.3 之前，非退化分布由三条独立的按分布路径提供服务，而非 §4.3 的单一 LUT（完整记录见 `doc/near-pole-area-measure-sampling.md`）：

**路径：瑞利分布（Rayleigh）（余纬度 < 0.5°）。** 在极点附近，雅可比因子与高斯分布结合后在切平面上给出瑞利分布。采样器抽取二维高斯样本，无需拒绝即可产生正确的球面密度。精确，每个样本 O(1)。

**路径：通用拒绝采样（generic rejection）。** 适用于高斯（非瑞利情形）、之字形和拉普拉斯分布：

1. 从基础分布中抽取候选 θ。
2. 以概率 cos(φ)/M 接受，其中 φ 为经 `NormalizeLatitude` 折叠后的纬度，M 为 `ComputeJacobianEnvelope()` 计算的包络常数：

| 类型 | 包络常数 M | 覆盖范围 |
|------|-----------|----------|
| 高斯（Gaussian） | cos(max(\|μ\| − 3σ, 0)°) | 99.7% |
| 之字形（Zigzag） | cos(max(\|μ\| − A, 0)°) | 全幅度 |
| 拉普拉斯（Laplacian） | cos(max(\|μ\| − 5b, 0)°) | 99.3% |

各类型的候选生成：

- **高斯：** 正态变量 × σ + μ。每个候选 O(1)。
- **之字形：** θ = |A · sin(2πU) + B|，U ~ Uniform(0,1)。O(1)。
- **拉普拉斯：** 逆 CDF 方法：θ = μ − b · sign(U−0.5) · ln(1−2|U−0.5|)。O(1)。
- **均匀：** 从 [μ − w/2, μ + w/2] 中抽取。O(1)。

**路径：确定性（deterministic）。** 无需采样（即今天的 `kNoRandom`）。

拒绝路径的实测接受率：

- 赤道附近（天顶角 ≈ 90°）：~100%（cos φ ≈ 1）
- 中纬度（天顶角 ≈ 45°）：~70–80%
- 极点附近（天顶角 ≈ 5°）：~30–40%
- 之字形/拉普拉斯分布：接受率类似，由相同的 cos(φ)/M 公式决定

正是这个近极接受率崩溃的问题，促使该架构被 §4.3 的统一 LUT 取代——完整设计推理与退役记录见 `doc/near-pole-area-measure-sampling.md`。


## 6. 参考文献

- Noel, V. & Sassen, K. (2005). J. Appl. Meteor. Climatol., 44(5), 653–664.
- Noel, V. & Chepfer, H. (2004). J. Atmos. Sci., 61(16), 2073–2081.
- Stout, J. E. et al. (2024). Atmos. Chem. Phys., 24, 11133–11155.
- Zhong, X. et al. (2023). J. Atmos. Sci., 80(6), 1539–1556.
- Kostinski, A. B. et al. (2025). Front. Remote Sens., 6, 1548902.
- Borovoi, A. G. & Kustova, N. V. (2009). Geophys. Res. Lett., 36, L04804.
- Shoemake, K. (1992). Graphics Gems III, 124–132.
- Wood, A. T. A. (1994). Commun. Statist. — Simul. Comput., 23(1), 157–164.
